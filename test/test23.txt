var a : real = 20 ;
var b : int = 42;


proc pluto(x : int)
{
   var c = a + b;
   var d = b + a;
   if (a < b) then {
      return;
   }
}

proc paperino(x : char, ref y : [5] real) : *char
{
    y[1] = 99;
   return &x;
}

proc gigibagigi(ref x : char) : *char
{
   x = 'b';
   return &x;
}

proc barbagianni(ref x : *char) : *char
{
   var xx = 'b';
   x = &xx;
   return x;
}

proc mysidefx(ref x : char)
{
   x = 'a';
}

proc gambadilegno_id(a : [5] real) : [5] real
{
    return a;
}

proc main(k:real)
{
   var c = a + b;
   var d = b + a;
   pluto(b);
   var asd = 'c';

   var ar : [5] real = [1,2,3,4,5];
   var z = paperino('c', ref ar);
   ar[4] = 2;
   var ar2 = gambadilegno_id(ar);
  var el3 = ar2[3];

   var ll = 'c';
   mysidefx(ref ll);
   if (a < b) then {
      return;
   }
}