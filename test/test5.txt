var m = [2.3,3,4,5,[3]];
var z:int = m[2];

proc pippo (l:int) : char
{
   l(3);

   proc bella (m:string) : string
   {
      a + 5 = 4;
      return 4;
   };
   var hiu:char;
   hiu= bella("44");

   l = m[2];
}

proc main (x:int , m:real)
{
   writeInt(m);
   writeReal(x);
   var h = "ciao";
   pippo(h);
}

