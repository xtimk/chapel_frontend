proc paperoga(x : int){
    var z = x;
}
proc main(ref x: [3]int) : int
{
    var jjj = "pippo";

    var boolean = true;
    while(boolean)
    {
        boolean = boolean;
    }

    proc paperoga(x : int) : *[1,2,3] int
    {
        var z = x;
        var ar = [[[2,2,2],[2,2,2]]];
        return &ar;
    };



    var hhhhh = [*(paperoga('d' + 0))];
    var bella = &hhhhh;
    var semprepiubella = &bella;

    var testone = paperoga('c' + 9);

    for jjj in (5 < 4) ? ((**&*&*&testone)[3+9]) : ((hhhhh)[2,3])  do
   //for jjj in (**semprepiubella)[5+5] do
    {
        for jjj in jjj do
        {
            //jjj[2+3] = 2;
            //jjj = 6; 
            //return 5 + 5 -3 + jjj;
        }

        return 10;
        
    }
}