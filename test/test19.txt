var a : real = 20 ;
var b : int = 42;

proc foo(ref x : int){
   if (x < 3) then
   {
      return;
   }
   x = x + 10;
}

proc main(kk:real)
{
   var b = 5;
   foo(ref (b + 3));
   var c = a + b;
   var d = b + a;
   main(a + b);
   if (a < b) then {
      var c = 5;
      return;
   }
   var f : [1..3] int = [1,2,3,4];
   var k = 2.3;
   var pointer_k = &k;
   var new_k = *pointer_k;

   var z = &kk + 5;

   var t = 3;
   var asd = &t + 2.2;

   var op += 4;

   (t + 3) = 6;

   4;
   3 + 2;

   var r = 5;
   var a_r = &r;
   var w = a_r * 5;

   var cyc = 4 + cyc;

   
   
}

proc pluto(x : int) : real{ 
   return x + 1.2;
} 

proc pluto(x: real) : real { 
   return x ;
} 

proc bar(x : int) : real{ 
   return x + 1.2;
} 

proc bar(x: real) : int {
   return 32 ;
} 



proc anotherfun(x : int) : int {
   if (x < 3) then
   {
      return;
   }
   return x + 15;
}