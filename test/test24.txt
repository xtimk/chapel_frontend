proc sidefx(ref x : int)
{
    x = 5;
}

proc main()
{
    var k = 5;
    sidefx(ref k);
    var j = [1.2,2,3,4,5];
    var a = [6,7.2,8,9,10];

    var addr_a = &a;

    // a = j;

    j = *addr_a;
    
}