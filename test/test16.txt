var a : real = 20 ;
var b : int = 42;


proc pluto(x : int)
{
   var c = a + b;
   var d = b + a;
   if (a < b) then {
      return;
   }
}

proc main(k:real)
{
   var c = a + b;
   var d = b + a;
   pluto(ref b);
   if (a < b) then {
      return;
   }
}